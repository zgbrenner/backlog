name: Apply semantic trace lifecycle fixes

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply:
    if: false
    runs-on: ubuntu-24.04
    steps:
      - run: echo "repair payload is being refreshed"
