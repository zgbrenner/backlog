# convertd runtime dependencies. Freeze with PyInstaller (see sidecar/BUILD.md).
# Pinned to EXACT, mutually-compatible versions for reproducible on-prem builds.
# To regenerate: build a clean venv, upgrade deliberately, re-test
# convert/ocr/langid/salience/classify, then `pip freeze` the resolved set here
# (keep the [all] extra on markitdown).
markitdown[all]==0.1.2
pypdfium2==4.30.0
rapidocr-onnxruntime==1.3.24
onnxruntime==1.20.1
fasttext-wheel==0.9.2
gliclass==0.1.10
sentence-transformers==3.3.1
transformers==4.46.3
torch==2.5.1          # CPU wheel is fine; used by gliclass/ettin/VL fallback
Pillow==11.0.0
numpy==1.26.4
huggingface_hub==0.26.2  # used by ../models/download_models.py
