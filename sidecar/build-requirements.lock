# Build-only dependencies for the Windows convertd executable.
# Keep every direct and transitive PyInstaller dependency exact so rebuilding
# the same BackLog release cannot silently pick up a different freezer.
PyInstaller==6.21.0
pyinstaller-hooks-contrib==2026.6
altgraph==0.17.5
packaging==26.2
pefile==2024.8.26
pywin32-ctypes==0.2.3
setuptools==83.0.0
