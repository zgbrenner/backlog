# Pins the Rust toolchain for reproducible local and CI builds.
# rustup reads this automatically in this directory tree; no manual
# `rustup override` is needed. Bump deliberately and re-run `cargo test` +
# `cargo clippy` across all platforms before merging a version bump.
[toolchain]
channel = "1.94.1"
components = ["rustfmt", "clippy"]
