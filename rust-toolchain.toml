# Pins the Rust toolchain for reproducible local and CI builds.
# Bump deliberately and rerun fmt, clippy, tests, and Windows packaging.
[toolchain]
channel = "1.97.1"
components = ["rustfmt", "clippy"]
profile = "minimal"
