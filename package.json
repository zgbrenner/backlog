{
  "name": "backlog",
  "private": true,
  "version": "0.8.2",
  "description": "Local-first document naming and indexing pipeline (Tauri frontend).",
  "license": "SEE LICENSE IN LICENSE",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm run build",
    "check:release": "node --test scripts/release-contract.test.mjs scripts/portable-contract.test.mjs scripts/validate-release-workflow.test.mjs scripts/signed-release-policy.test.mjs && node scripts/validate-release-workflow.mjs",
    "harness": "vite --config scripts/ui-harness/vite.harness.config.ts",
    "harness:shots": "node scripts/ui-harness/shoot.mjs",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.1.0",
    "@tauri-apps/plugin-dialog": "^2.0.1",
    "@tauri-apps/plugin-process": "^2",
    "@tauri-apps/plugin-updater": "^2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0",
    "playwright": "^1.62.0",
    "typescript": "^5.6.0",
    "vite": "^8.2.0",
    "yaml": "2.9.0"
  }
}
